import React from 'react';
import MainContainer from '../components/MainContainer';

const TaskQueue = () => {
    return (
        <div>
             <MainContainer/>
        </div>
    );
};

export default TaskQueue;