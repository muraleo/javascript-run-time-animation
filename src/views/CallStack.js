import React from 'react';
import MainContainer from '../components/MainContainer';

const CallStack = () => {
    return (
        <div>
            <MainContainer/>
        </div>
    );
};

export default CallStack;
